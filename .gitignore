# Node modules
node_modules

# generated files
dist
_site
src/_includes/css
src/_includes/scripts

# cache
.cache

# secret data
.env

# Editor configs mm
.nova
.vscode
.DS_Store
_originals
_resources
_backup

# Local Netlify folder
.netlify

# Obsidian
.obsidian
.vault-nickname
# Vault tooling stays local (Bases, note templates), but the Web Clipper
# templates are tracked — they're the source of truth (see __project_docs/web-clipper-pattern.html).
src/_obsidian/*
!src/_obsidian/clipper/

/src/posts/articles/-drafts

.claude
.omc
_raw/
_generated/
_tests/
.github

# Local Strava/Livelox export scratch (import source, not site content)
__strava

# Project notes (kept locally, not in repo)
AGENTS.md
CLAUDE.md
DESIGN.md
IDEAS.md
LOG.md
TODO.md


# Netlify dev regenerates this Edge Functions bootstrap lockfile on every run;
# the production build (npm run build -> Eleventy) never uses Deno.
deno.lock
