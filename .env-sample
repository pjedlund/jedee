URL=http://localhost:8080
# webmention.io API token — enables build-time fetch of received webmentions
# (Phase 2). Production-only; leave blank and the section renders empty everywhere.
# Get it from the webmention.io dashboard (the johanedlund.se domain is registered)
# and set it in local .env + Netlify's build env.
WEBMENTION_IO_TOKEN=