{
  "$type": "borderWidth",
  "$description": "The site's border widths. `thin` is the hairline behind --border-thickness in variables.css, which button.css, code.css and every other bordered thing inherits. Read by tailwind.config.js (emits --border-width-*) and build-penpot-tokens.js (emits border.* into core/layout). Icon stroke weights are NOT this token — those belong to the artwork.",
  "thin": { "$value": "1px" }
}
