{
  "$type": "color",
  "$description": "Semantic color tokens that swap between light and dark themes. Names mirror the --color-* CSS variables in src/assets/css/global/base/variables.css. Values reference palette tokens from colors.json via {curly.brackets} so any palette change cascades automatically. `accent-orange` is deliberately the same value in both themes — variables.css sets it from orange-500 in light and dark alike, unlike accent-blue and accent-green, whose theme pair already reaches the design tools as the light_dark colors from colors.json. Pushed to Penpot's theme/light and theme/dark sets by build-penpot-tokens.js. `bg-head` is a flat hex rather than a reference: variables.css builds it with color-mix, which has no token equivalent, so this mirrors the resolved value and has to be re-resolved by hand if the mix changes.",
  "themes": {
    "light": {
      "bg":          "{color.base.lightest}",
      "bg-accent":   "#ffffff",
      "bg-accent-2": "{color.gray.300}",
      "rule":        "{color.base.rule}",
      "rule-shadow": "{color.base.rule}",
      "rule-highlight": "{color.base.white}",
      "bg-head": "#e0e1e4",
      "text":        "{color.base.darkest}",
      "text-subdued": "{color.base.subdued}",
      "text-accent": "{color.gray.700}",
      "headline":    "{color.base.darkest}",
      "logo-icon":   "{color.base.dark}",
      "accent-orange": "{color.orange.500}",
      "border-top":  "{color.base.light}"
    },
    "dark": {
      "bg":          "{color.gray.900}",
      "bg-accent":   "{color.gray.700}",
      "bg-accent-2": "{color.gray.600}",
      "rule":        "{color.base.rule-dark}",
      "rule-shadow": "{color.base.black}",
      "rule-highlight": "{color.gray.800}",
      "bg-head": "#2d2f34",
      "text":        "{color.gray.250}",
      "text-subdued": "{color.gray.300}",
      "text-accent": "{color.gray.200}",
      "headline":    "{color.gray.200}",
      "logo-icon":   "{color.orange.700}",
      "accent-orange": "{color.orange.500}",
      "border-top":  "{color.orange.900}"
    }
  }
}
