{# Webmention render slot — no-op stub. Phase 2+ will fetch and render received
   webmentions here. Include with `{% include "partials/webmentions.njk" %}` from
   post layouts; the include stays safe to call even while the renderer is empty. #}
